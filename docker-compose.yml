version: '3.8'
services:
  dnstwist-webapp:
    build:
      context: .
      dockerfile: webapp/Dockerfile
    ports:
      - "8000:8000"
    container_name: dnstwist-separated-webapp
    environment:
      - PORT=8000
    restart: unless-stopped 